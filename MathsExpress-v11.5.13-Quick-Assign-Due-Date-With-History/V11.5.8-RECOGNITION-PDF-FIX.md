# MathsExpress v11.5.8 — Recognition + PDF Fix

- Teacher Recognition now renders the `star` sticker as a single ★ icon instead of the word `star`.
- Cambridge textbook preview no longer uses an iframe blocked by the site security headers.
- The PDF preview now uses the browser PDF object viewer, with an Open in new tab fallback.
- CSP now allows same-origin PDF objects while keeping framing protection enabled.
- Cache/app assets bumped to v11.5.8.
